server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
  profiles:
    active: native
#    配置文件的配置方式
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
#          指定本地配置文件的存放路径